<!-- BEGIN HADA_AI_PROMPT: HADA_AI_Prompt_Maintenance_Rules_JP.txt -->
```text
// HADAプロンプト保守ルール Ver.1.0.2.20260909
// (C) Hada | HADA_AI_Prompt_Maintenance_Rules_JP.txt
目的: 意味維持,生成性能維持,保守性向上,冗長削減,トークン削減
基本方針: プロンプトを読み物ではなく実行用仕様として保守する,説明的・反復的・装飾的な文章を可能な限り圧縮する,意味・条件・優先順位・制約・実行手順を維持しつつ最短の表現を優先する
形式: (重要: ...),(維持: ...),(品質: ...),(禁止: ...),[重要],[維持],[品質],[禁止]
項目表記: "項目:"形式と"[項目]"形式の両方を許可する,"[項目]"はセクションスコープ定義であり次の"[項目]"まで継続適用する,"項目:"は非スコープラベルとして扱う,"()"は括弧終了までのローカルスコープとして扱う,既存フォーマットを維持する,勝手に相互変換しない,同一文書内では既存形式を優先する
対象定義: 対象名(特徴A,特徴B,特徴C)
[Markdown保守]
Markdownは情報構造として必要な場合のみ使用する,見出し・箇条書き・引用・コードブロック・テキストブロック等の装飾目的のMarkdownを圧縮する,プロンプト出力ではMarkdownのテキストブロック・ライティングブロック表現を使用または維持しない,`:::`ブロック構文または同等のラッパー構文を使用しない,保守後のプロンプトはコードブロック内へ直接配置可能なプレーンテキストとして出力する,Markdown見出しは内容が単独のセクション定義として機能する場合`#`見出しを`[項目]`へ変換する,変換例: `# 重要原則`→`[重要原則]`,`## 解析`→`[解析]`,階層が必要な場合は既存の意味を維持して最小限の見出し構造を残す,Markdownの`*`箇条書きは同一カテゴリ・同一スコープ内で可能な限り1行へ統合する,例: `* A\n* B\n* C`→`A,B,C`,箇条書き項目が独立した条件・手順・優先順位として区別必須の場合のみ個別行を維持する,番号付き箇条書きは連続する同一カテゴリなら`1. A 2. B 3. C`等の文章形式へ圧縮する,番号そのものが意味を持たない場合は削除する,引用記号`>`は引用の意味が不要なら削除し本文へ統合する,Markdown表は比較・対応関係の維持に必要な場合のみ残す,コードブロックは実行対象・構文・固定形式の保持に必要な場合のみ残す,装飾目的のMarkdownは削除する
[改行・空白保守]
余分な改行を削除する,2連続改行を原則禁止する,セクション間の空行は原則1行までとする,同一項目内の文章・条件・列挙は可能な限り1行へ統合する,短い見出し直後の不要な空行を削除する,箇条書きの各項目を機械的に改行しない,改行が意味境界・スコープ境界・コード構造・固定フォーマットを示す場合のみ維持する,連続空白を必要最小限にする,Markdown構文維持に不要な空白を削除する
[文章圧縮]
説明・解説・背景・目的の反復を削除する,「〜してください」「〜すること」「〜を行ってください」等の同義反復は可能な限り短い命令形・条件形へ圧縮する,「以下の〜」「次の〜」「最低限〜」等の導入文は意味がなければ削除する,同じ禁止・要求・条件を複数箇所で説明しない,同義表現を統合する,同一意味の文章と箇条書きを重複保持しない,例示は実行仕様として必要な場合のみ残す,修辞・強調・比喩・読み物的表現は意味を損なわない範囲で削除する,「最も重要なのは〜ではありません」のような説明的表現は直接的な命令・優先順位へ圧縮する,引用文が仕様そのものなら短い規則へ変換する,意味を持たない接続詞・副詞・前置きを削除する
[カテゴリ統合]
同一カテゴリの連続項目を1行へ統合する,同一スコープ内の列挙はコンマ区切りで圧縮する,同一対象への複数条件は括弧内へ統合する,同一禁止事項は`禁止:`へ統合する,同一確認事項は`確認:`へ統合する,同一出力条件は`出力:`へ統合する,同一手順の説明と箇条書きを統合して重複を削除する,カテゴリ統合によって優先順位・条件・例外が失われる場合は分離を維持する
[見出し保守]
Markdown見出しが単なるカテゴリ名の場合は`[項目]`形式へ変換する,見出し階層自体が処理順序・親子関係を示す場合は階層を維持する,見出し本文の冗長な説明を削除する,見出しと直後の説明が一体化できる場合は`[項目]説明`へ圧縮する,同一階層の細分化が実行上不要なら統合する
[列挙圧縮]
`* A\n* B\n* C`→`A,B,C`を基本とする,同一カテゴリの列挙を別行へ分散しない,長い列挙はカテゴリ名と括弧を用いて圧縮する,例: `確認対象: A,B,C,D`,手順番号は順序が実行条件となる場合のみ維持する,順序不要な番号は削除する,丸数字,ローマ数字,記号付き番号,全角数字を使用しない
[意味保持]
圧縮前後で目的,条件,制約,優先順位,例外,依存関係,処理順序,入力,出力,禁止事項を維持する,圧縮によって意味が曖昧になる場合は圧縮しない,独立した意味を持つ条件を単純な列挙として結合しない,曖昧な場合は推測せず要確認とする
[変換]
保守開始時に全文を検査する,フォーマット構造と区切り記号に含まれる非ASCII記号を半角ASCII記号へ変換する,全角括弧は"()"へ変換する,全角角括弧は"[]"へ変換する,全角コロンは":"へ変換する,全角カンマと読点は","へ変換する,全角句点は"."へ変換する,中点は","へ変換する,丸数字,ローマ数字,記号付き番号,全角数字を使用しない,番号が必要な場合は半角ASCII数字を使用する,変換後に既存フォーマット維持と圧縮規則を適用する,日本語本文は変換対象外とする
[保守優先順位]
意味保持>生成性能保持>実行順序・条件保持>既存フォーマット維持>冗長削減>トークン削減>装飾削減,既存フォーマットが冗長でも意味を持つ場合は維持する,意味を持たないMarkdown・改行・空白・反復・テキストブロックラッパーを積極的に削除する
[規約]
フォーマット変更禁止,ただし本ルールで明示した圧縮・Markdown変換は許可する,装飾罫線禁止,余分な空行禁止,同義表現統合,重複削除,説明文圧縮,カテゴリ毎に統合,2連続改行禁止,同カテゴリはコンマ区切りで圧縮,意味を変えない,Ver.メジャー.ミドル.マイナー.YYYYMMDD形式を使用する,新規プロンプトはVer.0.0.1とする,通常更新はマイナーバージョンのみ加算する,内容変更時は日付を更新する,メジャーは大規模仕様変更時のみ更新する,ミドルは仕様追加時のみ更新する,バージョン番号は常に自動保守する,整形変更,空白変更,句読点変更,順序変更などの非意味変更もすべてマイナー更新対象とする,ASCII制約はフォーマット構造と区切り記号のみに適用する,日本語本文の使用を許可する,構造記号と区切り記号は半角ASCIIのみを使用する,新規プロンプトに既存ブランド名を付与しない,明示された変更対象以外を変更しない,外部のテキストブロック・ライティングブロックラッパー構文を出力または維持しない
[Markdown圧縮の例]
`* A\n* B\n* C`→`A,B,C`,`### CURRENT\n現在実装されているもの。`→`[CURRENT]現在実装されているもの。`,`# 1. Project Definition`→`[Project Definition]`,同一スコープの`* A\n* B\n* C`→`確認対象:A,B,C`,`:::writing{...}\nPrompt text\n:::`→`Prompt text`
[禁止]
読み物としての可読性を優先して冗長な改行を残さない,Markdownの箇条書きを内容ごとに機械的に1行ずつ残さない,見出しを意味なく階層化しない,同じ意味の本文と箇条書きを併記しない,説明のためだけの文章を追加しない,一般的な文章表現へ書き換えない,ユーザーが指定していない仕様を追加しない,内容を推測して変更しない,保守対象プロンプトを`:::`または同等の外部テキストブロック・ライティングブロックラッパーで囲まない
[バージョン]
保守対象の既存バージョンを基準とする,非意味的な整形・圧縮・Markdown変換もマイナー更新とする,内容変更時は日付を更新する,今回のような保守ルール自体の仕様追加はマイナー更新とする
[出力]
全文出力,フォーマット維持,冗長改行禁止,可能な限り1行へ圧縮,ただし意味境界・スコープ境界・実行順序・固定構造を損なう場合は改行を維持する,外部のテキストブロック・ライティングブロック・同等のラッパー構文なしでプロンプト本文のみ出力する,内容追加禁止(ユーザーから明示的な追加,削除,修正,リファクタリング指示がある場合を除く),内容削除禁止(ユーザーから明示的な削除指示がある場合を除く),ただし冗長表現・重複・意味を持たないMarkdown装飾の削除は保守処理として許可する,バージョン自動保守,コードブロック内では不要な空行・インデントを削除する,出力前に全文を保守規則と圧縮規則で内部検査し違反があれば修正してから出力,テキストのみ出力
[スレッド制約]
このスレッドはプロンプト文の保守専用とする,画像生成,画像編集,画像生成ツールの実行を禁止する,画像生成用の完成プロンプトを作成または更新しても画像生成を実行しない```
<!-- END HADA_AI_PROMPT -->

# 1. 重要項目10項目比較

| 項目        | 通常生成版(ルール適用なし) | 通常生成後ルール適用版 | 生成時ルール適用版 |
| --------- | -------------- | ----------- | --------- |
| ① 情報量     | ◎              | ◎           | △         |
| ② 具体的指定   | ◎              | ◎           | △         |
| ③ 意味保持    | ◎              | ◎           | △         |
| ④ 生成性能    | ◎              | ◎           | △         |
| ⑤ 冗長性削減   | △              | ◎           | ◎         |
| ⑥ 構造整理    | △              | ◎           | ◎         |
| ⑦ 保守性     | △              | ◎           | ◎         |
| ⑧ 情報欠落リスク | 低              | 低           | **高**     |
| ⑨ 文字数削減   | 基準             | 小           | 大を狙いやすい   |
| ⑩ 総合評価    | ◎              | **◎ 最適**    | △         |

# 2. 検証で確認されたこと

* **① 詳細なターゲットプロンプトを先に作ることが重要** — 生成に必要な具体的情報を最初から保持できる。
* **② 後からプロンプト保守ルールを適用しても情報をほぼ維持できる** — 構造整理と重複排除が中心になるため安全性が高い。
* **③ 最初からプロンプト保守ルールを適用すると情報欠落が起こり得る** — AIが「保守・圧縮」を「要約」と誤解する可能性がある。
* **④ 上位概念への置換が最大の危険** — 具体的な部位・形状・位置などを抽象化すると、ターゲットプロンプトの生成制御能力が低下する。
* **⑤ 文字数の圧縮と情報の圧縮は別物** — 改行・空白・完全重複の削減は安全だが、具体情報の削除は危険。
* **⑥ 通常生成後ルール適用版は非常に効率が良い** — 元の詳細さを保ちながら、管理しやすい形式へ整理できる。
* **⑦ 文字数削減効果そのものは小さい** — 今回は通常生成版→通常生成後ルール適用版で約1.8%程度の削減だった。
* **⑧ 6K文字前後から長文出力のリスクを意識する価値がある** — 長文化すると圧縮時に情報欠落が起きやすくなる。
* **⑨ 10K文字を超える領域ではさらに破綻リスクが高まる** — 長大なターゲットプロンプトを無理に一括保守しない方が安全。
* **⑩ 最適なのは「生成→保守→改良」の分離運用** — 新規作成と保守を同じコンテキストに最初から混在させない。

# 3. 推奨使用法

**新規ターゲットプロンプトは、新規スレッドでプロンプト保守ルールを適用せず通常生成する。**

完成したターゲットプロンプトを確認した後、**プロンプト保守ルールをスレッドに読み込ませ、既存プロンプトへ適用する。**

以後は、

**通常生成 → プロンプト保守ルール適用 → 改良 → 保守**

の順で運用する。

### メリット

* 初期生成時の情報量・創造性を最大化
* 保守時の情報欠落を最小化
* 重複・不要な改行・構造を整理できる
* プロンプトを簡潔かつ管理しやすく維持できる
* ルール適用による「要約化・抽象化」の事故を避けやすい
* 長文化した場合も、**6K〜8K文字程度を一つの実用上の目安**として管理できる
* 10K文字級まで無理に膨らませず、必要なら分割・整理を検討する
